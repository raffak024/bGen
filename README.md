# bGen

Unbekannt

## Lokale Entwicklung

```bash
# Je nach Projekttyp:
npm install && npm start  # Node.js
pip install -r requirements.txt && python main.py  # Python
```

## Docker Deployment

```bash
# Bauen und starten
docker compose up -d

# Logs anzeigen
docker compose logs -f

# Stoppen
docker compose down
```

## Zugriff

- Lokal: http://bgen.pi.local
- Health-Check: http://bgen.pi.local/health

---
Generiert mit App-Wizard
